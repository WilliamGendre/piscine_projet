{% extends 'publicView/base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block content %}

    <main id="home">
        <section>
            <h2><a href="{{ path('illustration_all') }}">Dernière mise en ligne</a></h2>
            <article class="illustrationHome">
                {% for illustration in illustrations %}
                    <div class="thumbnail thumbnailHome">
                        <a href="{{ path('public_illustration', {'id': illustration.id}) }}"><img src="{{ asset('uploadThumbnail/'~ illustration.thumbnail) }}" alt="{{ illustration.name }}"></a>
                    </div>
                {% endfor %}
            </article>
            <h2><a href="">Les plus visités de la semaines</a></h2>
            <article class="illustrationHome">
            </article>
            <h2><a href="">Les plus visités</a></h2>
            <article class="illustrationHome">
            </article>
        </section>
    </main>

{% endblock %}